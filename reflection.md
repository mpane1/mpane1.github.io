#reflection
1. When a http request is made from a browser, the browser will first look up the domain of the site, then the request will be sent to the Github server that is hosting my repo. Then the Github pages will return the HTML, CSS and image file back to the browser.

2. Docker containers can run dynamic applications like databases while github pages only hosts static content like HTMl and CSS.